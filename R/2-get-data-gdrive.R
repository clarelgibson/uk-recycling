# SUMMARY ######################################################################
# This script retrieves the data required for the project

# SETUP ########################################################################
# > Packages ===================================================================
library(here)

# > Scripts ====================================================================
source(here("R/1-utils.R"))

# > Params =====================================================================
folder = "~/uk-recycling"

# GET DATA #####################################################################
# > LAD ========================================================================
lad_src <- read_drive_file(
  folder = folder,
  filename = "lad23.csv"
)

# > Recycling ==================================================================
recycling_src <- read_drive_file(
  folder = folder,
  filename = "recycling-by-lad-2021.xlsx")
